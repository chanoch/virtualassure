import React from 'react';

export default class Triangle extends React.PureComponent {
    constructor(props) {
        super(props);
    }
    render() {
        return (    <hr className="triangle" />    )
    }
}
